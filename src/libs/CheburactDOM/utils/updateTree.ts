import { IElement } from 'libs/Cheburact/types';
import { IFiberNode } from 'libs/CheburactDOM/types';

export default function updateTree(
    $parent: HTMLElement,
    oldFiber: Array<IFiberNode> | null,
    newElement: IElement | null = null,
): Array<IFiberNode> | null {
  // TODO;
  return null;
}
